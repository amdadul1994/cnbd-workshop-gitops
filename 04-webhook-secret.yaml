apiVersion: v1
kind: Secret
metadata:
  name: cloudnative-gitops-webhook
  namespace: argocd
type: Opaque
stringData:
  # Webhook secret token - use a strong random string
  # This secret will be used to authenticate webhook requests from GitHub
  # 
  # To generate a secure random secret, run one of these commands:
  #   openssl rand -base64 32
  #   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
  #   head -c 32 /dev/urandom | base64
  #
  # IMPORTANT: Replace CHANGE_ME_TO_RANDOM_SECRET with your generated secret
  # and use the SAME secret when configuring the GitHub webhook
  secret: lv4Np5cE+oh0Kp7PvKpHUypF0VXy7qVjyRpD5sxPtF0=